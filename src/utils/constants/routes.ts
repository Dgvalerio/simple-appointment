interface Routes {
  home: string;
  signIn: string;
  signUp: string;
  addAppointment: string;
}

export const routes: Routes = {
  home: '/',
  signIn: '/sign-in',
  signUp: '/sign-up',
  addAppointment: '/appointment/create',
};
